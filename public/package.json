{
  "name": "commit-formatter",
  "version": "1.0.0",
  "description": "Formata commits locais no navegador para o padrão USTIBB",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node smoke-test.mjs"
  },
  "dependencies": {
    "isomorphic-git": "^1.36.1"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
